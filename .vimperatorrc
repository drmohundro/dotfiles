" display the menu, but not the toolbar
set guioptions=m

" highlight searches
set hlsearch

" undo stupid changes to backspace key
map <BS> <Esc>:back<cr>
map <S-BS> <Esc>:forward<cr>

" disable bell
set visualbell
