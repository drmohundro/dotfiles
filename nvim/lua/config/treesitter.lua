require('nvim-treesitter.configs').setup({
  highlight = {
    enable = true,
  },
  ensure_installed = 'maintained',
  indent = {
    enable = true,
  },
  rainbow = {
    enable = true,
    extended_mode = true,
  },
})
