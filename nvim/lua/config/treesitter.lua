require('nvim-treesitter.configs').setup({
  highlight = {
    enable = true,
  },
  ensure_installed = 'all',
  indent = {
    enable = true,
  },
  rainbow = {
    enable = true,
    extended_mode = true,
  },
})
